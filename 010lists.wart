(def pos(test s)
  (if (functionp test)
    (position-if test s)
    (position test s)))

(synonym len length
         rev reverse
         join append
         keep remove-if-not
         trunc truncate)
