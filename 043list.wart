def (list ... args)
  args

alias cadr car:cdr
alias cddr cdr:cdr

def (pair seq)
  (if
    no.seq
      nil
    ~cdr.seq
      (list:list car.seq)
    :else
      (cons (list car.seq cadr.seq)
            (pair cddr.seq)))

def (map f seq)
  if seq
    (cons (f car.seq)
          (map f cdr.seq))
