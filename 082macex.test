mac (foomex x)
  `(+ ,x 1)
(test "macex works"
  :valueof (macex '(foomex a))
  :should be '(+ a 1))

mac (foomex2 x)
  pr 3
  `(+ ,x 1)
(test "macex has macro-time side-effects"
  :valueof (with outstring (macex '(foomex2 a)))
  :should be "3")
