I. ctrl-d quits
^D

II. 2 newlines eval
prn 3<Enter>
<Enter>
3
=> 3

III. Fully-parenthesized form immediately evals
(prn "world")<Enter>
=> "world"
34<Enter>
=> 34

IV. Indent-based multi-line form
def (foo)<Enter>
  34<Enter>
<Enter>
=> (object function ..)

V. Add a comment at the prompt.
(prn "world") # this is a comment!<Enter>

VI. Two expressions on a single line:
() 34<Enter>
=> nil
=> 34

VII. Comments and newlines silently wait for next input
# skip<Enter>

VIII. Partial expr waits to complete:
(def (foo)<Enter>
<Enter>
34)<Enter>
=> (object function ..)

IX. Expr and partial expr and comment:
() (def (foo)<Enter>
=> nil
  34) # abc<Enter>
<Enter>
=> (object function ..)
