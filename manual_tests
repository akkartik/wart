I. ctrl-d quits
^D

III. Fully-parenthesized form immediately evals
(prn "world")<Enter>
=> "world"
34<Enter>
=> 34

IV. Indent-based multi-line form
def (foo)<Enter>
  34<Enter>
<Enter>
=> (object function ..)

V. Add a comment at the prompt.
(prn "world") # this is a comment!<Enter>

VI. Two expressions on a single line:
() 34<Enter>
=> nil
=> 34

VII. Three expressions on a single line:
() 3 4<Enter>
=> nil
=> 3
=> 4

VIII. Comments and newlines silently wait for next input
# skip<Enter>

IX. Partial expr waits to complete:
(def (foo)<Enter>
<Enter>
34)<Enter>
=> (object function ..)

X. Expr and partial expr and comment:
() (def (foo)<Enter>
=> nil
  34) # abc<Enter>
<Enter>
=> (object function ..)

XI. Implicit parens after explicit
(prn 'a)
=> 'a
prn 'a
=> 'a
