Some languages have statements as well as expressions, but this causes
unnecessary boilerplate:

  if (x > 3)
    (f x-3)
    (f x)

Allowing everything to be an expression returning a value allows us to remove
the repetition:

  (f (if (x > 3) x-3 x))
