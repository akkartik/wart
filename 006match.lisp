(defun match(a b)
  (or (equal a b)
      (and (consp a) (consp b)
           (match (car a) (car b))
           (match (cdr a) (cdr b)))))

(extend match(a b) :case (equal b '_)
  t)
(extend match(a b) :case (and (consp a) (consp b)
                           (equal (car b) '_..))
  t)
