(test "sym works with one arg"
 :valueof (sym "abc")
 :should be 'abc)

(test "sym works on empty string"
 :valueof (sym "")
 :should be nil)

(test "sym works with multiple args"
 :valueof (str:sym "abc" 42 :def) ; keyword retains colon
 :should be "abc42:def")

(test "keyword? works"
 :valueof (keyword? 'abc)
 :should be nil)

(test "keyword? works - 2"
 :valueof (keyword? :abc)
 :should ~be nil)

(test "sym converts keywords"
 :valueof (sym :abc)
 :should be 'abc) ; keyword loses colon
